# Environment Configuration
# Copy this file to .env and update values as needed

# Backend Configuration
PORT=8080
ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=password
DB_NAME=canvasworld

# Frontend Configuration
VITE_PORT=5173
BACKEND_PORT=8080

# Production URLs (for SSR meta tags)
PROD_URL=https://canvasworld.dev
