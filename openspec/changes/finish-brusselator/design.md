## Context

Issue #31. Catalog stub + broken page.

## Decisions

1. Discrete map from catalog description (not the unfinished Gray-Scott grid in `brusselator copy.tsx`)
2. Use `createAttractorPage`
3. Delete `brusselator copy.tsx` as dead WIP

## Risks

- [No thumb PNG yet] → gallery fallback until `pnpm thumbs -- --slug brusselator`
- [Param defaults] → tune a/b/seed so the orbit stays visible
