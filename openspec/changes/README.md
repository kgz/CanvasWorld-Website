# OpenSpec changes

Active proposals live here as `openspec/changes/<change-name>/`.

Archived changes move to `openspec/changes/archive/YYYY-MM-DD-<name>/`.

Start a change: `/opsx:propose "<idea>"` (on `master`).
Implement: `/opsx:apply` (on `feature/<issue>-<slug>`).
