## 1. Spec & branch

- [x] 1.1 OpenSpec artifacts for `frontpage-od-port`
- [x] 1.2 Branch `feature/24-frontpage-od-port` from master

## 2. Frontend port

- [x] 2.1 Add frontpage CSS module (OD tokens + hero/gallery/footer)
- [x] 2.2 Add hero ink canvas component
- [x] 2.3 Rewrite `index-new.tsx` to OD layout wired to `routes` + `genPath`
- [x] 2.4 Random CTA + scroll nav + reduced-motion behaviour

## 3. Verify

- [x] 3.1 `pnpm run build` in `packages/frontend` (vite build OK; pre-existing `tsc` errors elsewhere)
- [ ] 3.2 Manual smoke: home hero + gallery links
