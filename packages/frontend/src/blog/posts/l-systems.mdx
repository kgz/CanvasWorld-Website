export const meta = {
	slug: 'l-systems',
	title: 'L-systems: a grammar for trees',
	tag: 'Fractals',
	excerpt:
		'Rewrite a string a dozen times, hand it to a turtle, and a branching structure falls out — no geometry required, just substitution rules.',
	dateLabel: 'May 2026',
	readMinutes: 5,
	thumbSlug: 'sierpinski_triangle',
	order: 60,
}


Rewrite a string a dozen times, hand it to a turtle, and a branching structure falls out — no geometry required, just substitution rules.

Related fractal grammar on the site includes the Sierpinski construction — different production rules, same idea of discrete rewrite → drawn form.

<VizEmbed slug="sierpinski_triangle" label="Sierpinski" />

<Callout>
  <strong>Next</strong>
  Open Sierpinski full-page to scrub recursion depth and watch the gasket fill in.
</Callout>
